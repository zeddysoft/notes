kubernetes offers things like
service discovery
# Introduction


# Side note
On macOS, docker daemon is installed as a VM and docker client executable is made available on the host OS that then communicates with the daemon inside the VM.